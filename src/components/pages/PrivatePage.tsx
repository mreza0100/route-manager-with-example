export default function PrivatePage() {
	return (
		<main>
			<h1>if you can see this it means you are a authenticated user</h1>
			<h2>and all handled with "withPrivateRoute" HOC in driver</h2>
		</main>
	);
}
